---
phase: 02-content
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - noesis/src/__tests__/topics.test.ts
  - noesis/src/__tests__/topic-content.test.ts
  - noesis/src/__tests__/diagrams.test.tsx
  - noesis/src/__tests__/video.test.tsx
  - noesis/e2e/topics.spec.ts
  - noesis/e2e/links.spec.ts
  - noesis/package.json
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06
  - CONT-07
  - CONT-08
  - DISC-03
  - BRAND-02

must_haves:
  truths:
    - "All test stubs exist and report pending (not failing) before any implementation"
    - "react-lite-youtube-embed is installed and importable"
    - "npm run test completes without errors (pending tests pass, no red)"
  artifacts:
    - path: "noesis/src/__tests__/topics.test.ts"
      provides: "Unit tests for topics array structure and slug uniqueness"
    - path: "noesis/src/__tests__/topic-content.test.ts"
      provides: "Unit tests for TopicContent schema completeness across all 7 topics"
    - path: "noesis/src/__tests__/diagrams.test.tsx"
      provides: "Unit tests for all 7 SVG diagram components rendering without errors"
    - path: "noesis/src/__tests__/video.test.tsx"
      provides: "Unit tests for VideoSection component rendering with YouTube ID"
    - path: "noesis/e2e/topics.spec.ts"
      provides: "E2E tests for all 7 topic pages loading, progress strip, content visibility"
    - path: "noesis/e2e/links.spec.ts"
      provides: "E2E tests for cross-topic links resolving without 404"
  key_links:
    - from: "noesis/src/__tests__/topic-content.test.ts"
      to: "noesis/src/lib/topic-content.ts"
      via: "import topicContent"
      pattern: "import.*topic-content"
    - from: "noesis/src/__tests__/diagrams.test.tsx"
      to: "noesis/src/components/diagrams/"
      via: "import diagram components"
      pattern: "import.*Diagram"
---

<objective>
Create Wave 0 test infrastructure: all test stubs that will turn green as implementation plans execute. Also install react-lite-youtube-embed.

Purpose: Establish the Nyquist feedback loop before any implementation. Tests are stubs (describe.todo / test.todo) that report pending, not failing — matching the Phase 1 pattern established for test scaffolding.
Output: 6 test files with todo stubs + react-lite-youtube-embed in package.json.
</objective>

<execution_context>
@/Users/rajaonapple/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rajaonapple/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content/02-VALIDATION.md

<interfaces>
<!-- Existing test patterns from Phase 1 (follow these exactly) -->
<!-- From noesis/src/__tests__/components/GlassCard.test.tsx pattern: -->
```typescript
// Phase 1 pattern: describe.todo for stubs
import { describe } from 'vitest';

describe.todo('ComponentName');
```

<!-- From noesis/e2e/homepage.spec.ts pattern: -->
```typescript
import { test, expect } from '@playwright/test';

test.describe('Feature', () => {
  test('behavior', async ({ page }) => { ... });
});
```

<!-- topics array from noesis/src/lib/topics.ts: -->
```typescript
export const topics: Topic[] = [
  { slug: 'data-sources', title: 'Data Sources', ... },
  { slug: 'data-integration', title: 'Data Integration', ... },
  { slug: 'data-quality', title: 'Data Quality', ... },
  { slug: 'master-data-management', title: 'Master Data Management', ... },
  { slug: 'reverse-integration', title: 'Reverse Integration', ... },
  { slug: 'data-governance', title: 'Data Governance', ... },
  { slug: 'ai-in-data-management', title: 'AI in Data Management', ... },
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-lite-youtube-embed and create unit test stubs</name>
  <files>
    noesis/package.json,
    noesis/src/__tests__/topics.test.ts,
    noesis/src/__tests__/topic-content.test.ts,
    noesis/src/__tests__/diagrams.test.tsx,
    noesis/src/__tests__/video.test.tsx
  </files>
  <action>
    First, install react-lite-youtube-embed: run `cd noesis && npm install react-lite-youtube-embed` from the project root.

    Then create 4 unit test stubs in noesis/src/__tests__/ (NOT inside the components/ subdirectory — these are new top-level files).

    **noesis/src/__tests__/topics.test.ts** — Use describe/test blocks (not todo) since topics.ts already exists:
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { topics } from '@/lib/topics';

    describe('topics array', () => {
      it('contains exactly 7 topics', () => {
        expect(topics).toHaveLength(7);
      });

      it('all slugs are unique', () => {
        const slugs = topics.map((t) => t.slug);
        const unique = new Set(slugs);
        expect(unique.size).toBe(7);
      });

      it('all topics have required fields', () => {
        for (const topic of topics) {
          expect(topic.slug).toBeTruthy();
          expect(topic.title).toBeTruthy();
          expect(topic.description).toBeTruthy();
          expect(topic.Icon).toBeDefined();
        }
      });

      it('slugs match expected learning path order', () => {
        const expectedOrder = [
          'data-sources',
          'data-integration',
          'data-quality',
          'master-data-management',
          'reverse-integration',
          'data-governance',
          'ai-in-data-management',
        ];
        expect(topics.map((t) => t.slug)).toEqual(expectedOrder);
      });
    });
    ```

    **noesis/src/__tests__/topic-content.test.ts** — Use describe.todo since topic-content.ts doesn't exist yet:
    ```typescript
    import { describe } from 'vitest';

    describe.todo('TopicContent schema — all 7 topics have mandatory sections');
    describe.todo('TopicContent — data-sources entry');
    describe.todo('TopicContent — data-integration entry');
    describe.todo('TopicContent — data-quality entry');
    describe.todo('TopicContent — master-data-management entry');
    describe.todo('TopicContent — reverse-integration entry');
    describe.todo('TopicContent — data-governance entry');
    describe.todo('TopicContent — ai-in-data-management entry');
    ```

    **noesis/src/__tests__/diagrams.test.tsx** — Use describe.todo since diagram components don't exist yet:
    ```typescript
    import { describe } from 'vitest';

    describe.todo('DataSourcesDiagram renders without errors');
    describe.todo('DataIntegrationDiagram renders without errors');
    describe.todo('DataQualityDiagram renders without errors');
    describe.todo('MDMDiagram renders without errors');
    describe.todo('ReverseIntegrationDiagram renders without errors');
    describe.todo('DataGovernanceDiagram renders without errors');
    describe.todo('AIDataManagementDiagram renders without errors');
    ```

    **noesis/src/__tests__/video.test.tsx** — Use describe.todo since VideoSection doesn't exist yet:
    ```typescript
    import { describe } from 'vitest';

    describe.todo('VideoSection renders with valid YouTube ID prop');
    describe.todo('VideoSection renders without crashing when ID is empty string');
    ```
  </action>
  <verify>
    <automated>cd /Users/rajaonapple/Documents/ClaudeCode/NotebookLM/noesis && npm run test 2>&1 | tail -20</automated>
  </verify>
  <done>npm run test passes (topics.test.ts shows 4 passing tests; topic-content, diagrams, video show todo/pending — no red failures). react-lite-youtube-embed appears in package.json dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Create E2E test stubs for topic pages and cross-topic links</name>
  <files>
    noesis/e2e/topics.spec.ts,
    noesis/e2e/links.spec.ts
  </files>
  <action>
    Create 2 new Playwright E2E test files. These are stubs with skip markers (test.skip) so they don't fail before implementation. Use skip, not todo — Playwright's test.todo() has different semantics than vitest.

    **noesis/e2e/topics.spec.ts:**
    ```typescript
    import { test, expect } from '@playwright/test';

    const TOPIC_SLUGS = [
      'data-sources',
      'data-integration',
      'data-quality',
      'master-data-management',
      'reverse-integration',
      'data-governance',
      'ai-in-data-management',
    ];

    test.describe('Topic pages', () => {
      for (const slug of TOPIC_SLUGS) {
        test.skip(`/topics/${slug} loads and shows content`, async ({ page }) => {
          await page.goto(`/topics/${slug}`);
          await expect(page).not.toHaveURL(/404/);
          // Overview section visible
          await expect(page.getByRole('heading', { name: 'Overview' })).toBeVisible();
          // How AI Applies section visible
          await expect(page.getByRole('heading', { name: 'How AI Applies' })).toBeVisible();
          // From the Field callout visible
          await expect(page.getByText('From the Field')).toBeVisible();
          // Architecture/diagram section visible
          await expect(page.getByRole('heading', { name: 'Architecture' })).toBeVisible();
          // Progress strip renders (7 steps)
          const progressStrip = page.getByRole('navigation', { name: 'Learning path' });
          await expect(progressStrip).toBeVisible();
          const steps = progressStrip.getByRole('listitem');
          await expect(steps).toHaveCount(7);
        });
      }

      test.skip('progress strip current step is highlighted', async ({ page }) => {
        await page.goto('/topics/data-quality');
        const strip = page.getByRole('navigation', { name: 'Learning path' });
        // The "Data Quality" step should have aria-current="step"
        const activeStep = strip.locator('[aria-current="step"]');
        await expect(activeStep).toBeVisible();
      });

      test.skip('progress strip steps are all clickable links', async ({ page }) => {
        await page.goto('/topics/data-sources');
        const strip = page.getByRole('navigation', { name: 'Learning path' });
        const links = strip.getByRole('link');
        await expect(links).toHaveCount(7);
      });

      test.skip('"Where to Go Next" section renders and has clickable links', async ({ page }) => {
        await page.goto('/topics/data-sources');
        await expect(page.getByRole('heading', { name: 'Where to Go Next' })).toBeVisible();
        const nextLinks = page.locator('section').filter({ hasText: 'Where to Go Next' }).getByRole('link');
        await expect(nextLinks.first()).toBeVisible();
      });
    });
    ```

    **noesis/e2e/links.spec.ts:**
    ```typescript
    import { test, expect } from '@playwright/test';

    test.describe('Cross-topic links', () => {
      test.skip('cross-topic inline links in data-quality navigate correctly', async ({ page }) => {
        await page.goto('/topics/data-quality');
        // Find first cross-topic link in the content area (not progress strip)
        const contentLinks = page.locator('main a[href^="/topics/"]');
        const count = await contentLinks.count();
        expect(count).toBeGreaterThan(0);
        // Click the first one and confirm no 404
        await contentLinks.first().click();
        await expect(page).not.toHaveURL(/404/);
      });

      test.skip('cross-topic inline links in master-data-management navigate correctly', async ({ page }) => {
        await page.goto('/topics/master-data-management');
        const contentLinks = page.locator('main a[href^="/topics/"]');
        const count = await contentLinks.count();
        expect(count).toBeGreaterThan(0);
      });

      test.skip('all "Where to Go Next" links resolve without 404', async ({ page }) => {
        await page.goto('/topics/data-integration');
        const nextSection = page.locator('section').filter({ hasText: 'Where to Go Next' });
        const links = nextSection.getByRole('link');
        const hrefs: (string | null)[] = [];
        const count = await links.count();
        for (let i = 0; i < count; i++) {
          hrefs.push(await links.nth(i).getAttribute('href'));
        }
        for (const href of hrefs) {
          if (href) {
            const response = await page.goto(href);
            expect(response?.status()).not.toBe(404);
          }
        }
      });
    });
    ```

    Note: The existing e2e/homepage.spec.ts from Phase 1 will be extended in Plan 07 to cover the "Start Here" CTA and About section — do NOT modify it in this plan.
  </action>
  <verify>
    <automated>cd /Users/rajaonapple/Documents/ClaudeCode/NotebookLM/noesis && npx playwright test e2e/topics.spec.ts e2e/links.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>Both e2e test files exist. All tests in topics.spec.ts and links.spec.ts are skipped (not failing). Playwright reports 0 failures, N skipped.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `cd noesis && npm run test` — passes with 4 passing + multiple todos/pending, zero red
- `cd noesis && npx playwright test e2e/topics.spec.ts e2e/links.spec.ts` — all skipped, zero failures
- `cat noesis/package.json | grep react-lite-youtube` — shows package in dependencies
</verification>

<success_criteria>
- react-lite-youtube-embed in noesis/package.json dependencies
- 4 unit test files in noesis/src/__tests__/ (topics.test.ts has real tests; others are todos)
- 2 e2e test files in noesis/e2e/ with skip guards
- npm run test: green (no failures, todos/pending acceptable)
- Playwright: 0 failures on new test files (all skipped)
</success_criteria>

<output>
After completion, create `.planning/phases/02-content/02-01-SUMMARY.md`
</output>
