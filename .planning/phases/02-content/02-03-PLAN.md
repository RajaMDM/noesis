---
phase: 02-content
plan: 03
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - noesis/src/components/diagrams/DataSourcesDiagram.tsx
  - noesis/src/components/diagrams/DataIntegrationDiagram.tsx
  - noesis/src/components/diagrams/DataQualityDiagram.tsx
  - noesis/src/components/diagrams/MDMDiagram.tsx
  - noesis/src/components/diagrams/ReverseIntegrationDiagram.tsx
  - noesis/src/components/diagrams/DataGovernanceDiagram.tsx
  - noesis/src/components/diagrams/AIDataManagementDiagram.tsx
  - noesis/src/__tests__/diagrams.test.tsx
autonomous: true
requirements:
  - CONT-06

must_haves:
  truths:
    - "All 7 SVG diagram components exist and are importable"
    - "Each diagram renders inline SVG using CSS variables (not hardcoded hex colors)"
    - "Each diagram uses viewBox for responsive scaling — fills container width"
    - "All 7 diagrams render without errors in the test suite"
  artifacts:
    - path: "noesis/src/components/diagrams/DataSourcesDiagram.tsx"
      provides: "Source taxonomy tree SVG"
    - path: "noesis/src/components/diagrams/DataIntegrationDiagram.tsx"
      provides: "ETL/ELT pipeline flow SVG"
    - path: "noesis/src/components/diagrams/DataQualityDiagram.tsx"
      provides: "Quality dimensions wheel SVG"
    - path: "noesis/src/components/diagrams/MDMDiagram.tsx"
      provides: "Hub-and-spoke entity model SVG"
    - path: "noesis/src/components/diagrams/ReverseIntegrationDiagram.tsx"
      provides: "Bidirectional data flow SVG"
    - path: "noesis/src/components/diagrams/DataGovernanceDiagram.tsx"
      provides: "Governance framework layers SVG"
    - path: "noesis/src/components/diagrams/AIDataManagementDiagram.tsx"
      provides: "AI capability overlay on data stack SVG"
  key_links:
    - from: "noesis/src/components/diagrams/*.tsx"
      to: "noesis/src/styles/globals.css"
      via: "CSS variables in style props"
      pattern: "var\\(--color-"
---

<objective>
Build all 7 custom SVG diagram React components. These render as inline SVG using CSS variables so they stay on-brand (noir + electric blue) without any design tool exports.

Purpose: Each topic page will import exactly one diagram component. All 7 must be ready before Plan 06 wires the topic page template.
Output: 7 TSX files in noesis/src/components/diagrams/, diagrams.test.tsx updated with real tests.
</objective>

<execution_context>
@/Users/rajaonapple/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rajaonapple/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content/02-CONTEXT.md
@.planning/phases/02-content/02-RESEARCH.md
@.planning/phases/02-content/02-01-SUMMARY.md

<interfaces>
<!-- CSS variables from noesis/src/styles/globals.css — use these in every diagram: -->
// --color-noir           (background: near-black)
// --color-accent-blue    (electric blue for lines, nodes, labels)
// --color-text-secondary (softer white for labels)
// --color-glass-border   (subtle border for grouping boxes)
// --radius-md            (border radius for rounded rects)

<!-- SVG component pattern from RESEARCH.md: -->
```typescript
export function DiagramName() {
  const lineColor = 'var(--color-accent-blue)';
  const textColor = 'white';

  return (
    <svg
      viewBox="0 0 800 400"
      className="w-full max-w-4xl mx-auto my-8"
      style={{ backgroundColor: 'var(--color-noir)', borderRadius: 'var(--radius-md)' }}
      role="img"
      aria-label="Diagram description for screen readers"
    >
      {/* SVG content */}
    </svg>
  );
}
```

<!-- Key constraint: NO hardcoded hex colors. Always use CSS variable strings. -->
<!-- Do NOT animate diagrams — Phase 2 ships static. Interactive walkthroughs are v2. -->
<!-- CSS :hover is allowed for subtle hover effects (stroke-opacity change on nodes). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DataSourcesDiagram, DataIntegrationDiagram, DataQualityDiagram</name>
  <files>
    noesis/src/components/diagrams/DataSourcesDiagram.tsx,
    noesis/src/components/diagrams/DataIntegrationDiagram.tsx,
    noesis/src/components/diagrams/DataQualityDiagram.tsx
  </files>
  <action>
    Create the diagrams/ directory and the first 3 diagram components.

    **DataSourcesDiagram.tsx** — Source taxonomy tree:
    - Layout: 800×450 viewBox. Central root node "Enterprise Data" at center-top. Four branches going down: "Databases", "APIs", "Files", "Streams". Under each: 2-3 example leaf nodes (Databases → PostgreSQL, Oracle, MongoDB; APIs → REST, GraphQL, Kafka; Files → CSV, JSON, Parquet; Streams → Kafka, Kinesis, Event Hub).
    - Visual: circles for root + category nodes, rounded rects for leaf nodes. Lines connecting them. Electric blue strokes, white text labels. Subtle opacity (0.7) on leaf nodes to create visual hierarchy.
    - Aria label: "Data source taxonomy: Enterprise Data branches into Databases, APIs, Files, and Streams, each with specific examples."

    **DataIntegrationDiagram.tsx** — ETL/ELT pipeline flow:
    - Layout: 800×400 viewBox. Left column: 3 source system boxes (CRM, ERP, Files). Middle: "Extract" arrow → "Stage" box → fork with two paths: upper path "Transform → Load (ETL)" and lower path "Load → Transform (ELT)". Right: Data Warehouse box.
    - Visual: rectangles for systems, arrows (path with marker-end arrowhead), dashed line for the ETL/ELT fork. Label the fork clearly. Badge showing "Batch" on ETL path, "Streaming" option annotation.
    - Aria label: "ETL vs ELT pipeline: source systems extract to staging, then either transform-first (ETL) or load-first (ELT) into the data warehouse."

    **DataQualityDiagram.tsx** — Quality dimensions wheel:
    - Layout: 800×450 viewBox. Central circle "Fit for Purpose" at center (400, 225). Six dimension circles arranged around it at equal angles (60° apart). Dimensions: Accuracy, Completeness, Consistency, Uniqueness, Validity, Timeliness. Lines from center to each dimension.
    - Calculation of positions: r=160 from center. Angles: 270° (top), 330°, 30°, 90° (bottom), 150°, 210°. x = 400 + 160*cos(angle), y = 225 + 160*sin(angle).
    - Visual: center circle filled with accent blue (low opacity fill rgba), dimension circles with outline only, connecting lines with opacity 0.6.
    - Aria label: "Data quality dimensions wheel: six dimensions (Accuracy, Completeness, Consistency, Uniqueness, Validity, Timeliness) surrounding a central Fit for Purpose hub."

    All three diagrams:
    - Export as named function (not default export) to match import pattern
    - No TypeScript props — these are standalone visual components
    - Include `role="img"` and `aria-label` on the SVG element
    - SVG padding: use `<g transform="translate(20, 20)">` if needed for internal padding
    - DO NOT hardcode colors. All colors via CSS variable string references.
  </action>
  <verify>
    <automated>cd /Users/rajaonapple/Documents/ClaudeCode/NotebookLM/noesis && npm run build 2>&1 | grep -E "error|warning|compiled" | head -20</automated>
  </verify>
  <done>Three diagram files exist and TypeScript compiles without errors. Each diagram exports a named function component.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create MDMDiagram, ReverseIntegrationDiagram, DataGovernanceDiagram, AIDataManagementDiagram, then update diagrams.test.tsx</name>
  <files>
    noesis/src/components/diagrams/MDMDiagram.tsx,
    noesis/src/components/diagrams/ReverseIntegrationDiagram.tsx,
    noesis/src/components/diagrams/DataGovernanceDiagram.tsx,
    noesis/src/components/diagrams/AIDataManagementDiagram.tsx,
    noesis/src/__tests__/diagrams.test.tsx
  </files>
  <behavior>
    - All 7 diagram components render without throwing errors
    - Each diagram renders an SVG element
    - Each SVG has aria-label attribute
    - Test imports all 7 diagram components
  </behavior>
  <action>
    **MDMDiagram.tsx** — Hub-and-spoke entity model:
    - Layout: 800×460 viewBox. Central "Golden Record Hub" box at center. Seven spoke system ellipses arranged around it: CRM, ERP, eCommerce, Finance, HR, Logistics, Marketing. Bidirectional arrows (→) on each spoke. Three golden record type badges inside the hub: Customer, Product, Supplier.
    - Visual: Hub is a rounded rect with accent blue border and subtle fill. Spokes are smaller ellipses. Double-headed arrows use two arrowhead markers. Labels inside each spoke.
    - Aria label: "MDM hub-and-spoke model: central Golden Record Hub containing Customer, Product, and Supplier records, connected bidirectionally to 7 spoke systems: CRM, ERP, eCommerce, Finance, HR, Logistics, Marketing."

    **ReverseIntegrationDiagram.tsx** — Bidirectional data flow:
    - Layout: 800×400 viewBox. Three columns: left (Source Systems: CRM, ERP, Files → Warehouse), center (Data Warehouse / Intelligence Hub, labeled as such), right (Activation targets: CRM, Marketing, Ad Platform, Support Tool). Traditional ETL flows left→center (smaller, muted). Reverse integration flows center→right (prominent, electric blue arrows).
    - Highlight: "Reverse Integration" label on the right-side arrows with a badge. Center box is the largest, most prominent element.
    - Aria label: "Reverse integration flow: source systems feed data warehouse via ETL (left), warehouse activates enriched data to operational tools via reverse integration (right)."

    **DataGovernanceDiagram.tsx** — Framework layers:
    - Layout: 800×400 viewBox. Three horizontal bands stacked vertically. Bottom band: "Policies & Standards" (widest, foundation). Middle band: "Stewards & Councils + Data Catalog" (slightly narrower). Top band: "Compliance & Audit" (narrowest, pinnacle). Upward arrow from bottom to top labeled "Policy enforcement". Downward arrow from top to bottom labeled "Audit findings".
    - Visual: Each band is a rounded rect with different opacity of the glass bg. Labels centered in each band. Two arrows (up and down) on the right side of the diagram.
    - Aria label: "Governance framework pyramid: Policies and Standards at the foundation, Stewards and Councils plus Data Catalog in the middle, Compliance and Audit at the top."

    **AIDataManagementDiagram.tsx** — AI capability overlay on the data stack:
    - Layout: 800×420 viewBox. Three horizontal layers. Bottom: "Storage & Compute" (gray/muted). Middle: "Data Management Layer" — subdivided into four labeled sections: Quality, MDM, Governance, Integration (the four Phase 2 domains). Top: "AI / ML Layer" — subdivided into: Models, Feature Store, Vector DBs, Agents. Bidirectional arrows between Data Management and AI layers. One-way arrow from Storage up to Data Management.
    - Visual: AI layer has the most vivid electric blue. Data Management layer uses medium blue. Storage is muted. Bold "↕" annotation between the two top layers saying "mutual dependency."
    - Aria label: "AI and data management stack: storage and compute at base, data management layer (quality, MDM, governance, integration) in the middle, AI and ML layer (models, feature stores, vector databases, agents) at top with mutual dependency arrows."

    **Update noesis/src/__tests__/diagrams.test.tsx** with real render tests:
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { render } from '@testing-library/react';
    import { DataSourcesDiagram } from '@/components/diagrams/DataSourcesDiagram';
    import { DataIntegrationDiagram } from '@/components/diagrams/DataIntegrationDiagram';
    import { DataQualityDiagram } from '@/components/diagrams/DataQualityDiagram';
    import { MDMDiagram } from '@/components/diagrams/MDMDiagram';
    import { ReverseIntegrationDiagram } from '@/components/diagrams/ReverseIntegrationDiagram';
    import { DataGovernanceDiagram } from '@/components/diagrams/DataGovernanceDiagram';
    import { AIDataManagementDiagram } from '@/components/diagrams/AIDataManagementDiagram';

    const diagrams = [
      { name: 'DataSourcesDiagram', Component: DataSourcesDiagram },
      { name: 'DataIntegrationDiagram', Component: DataIntegrationDiagram },
      { name: 'DataQualityDiagram', Component: DataQualityDiagram },
      { name: 'MDMDiagram', Component: MDMDiagram },
      { name: 'ReverseIntegrationDiagram', Component: ReverseIntegrationDiagram },
      { name: 'DataGovernanceDiagram', Component: DataGovernanceDiagram },
      { name: 'AIDataManagementDiagram', Component: AIDataManagementDiagram },
    ];

    for (const { name, Component } of diagrams) {
      describe(name, () => {
        it('renders without throwing', () => {
          expect(() => render(<Component />)).not.toThrow();
        });

        it('renders an SVG element', () => {
          const { container } = render(<Component />);
          const svg = container.querySelector('svg');
          expect(svg).toBeTruthy();
        });

        it('SVG has aria-label', () => {
          const { container } = render(<Component />);
          const svg = container.querySelector('svg');
          expect(svg?.getAttribute('aria-label')).toBeTruthy();
        });
      });
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/rajaonapple/Documents/ClaudeCode/NotebookLM/noesis && npm run test -- --reporter=verbose 2>&1 | grep -E "diagrams|✓|✗|PASS|FAIL" | head -30</automated>
  </verify>
  <done>diagrams.test.tsx: all 21 tests passing (7 diagrams × 3 checks). All 7 diagram files exist in components/diagrams/. npm run test exits 0.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `ls noesis/src/components/diagrams/` — shows all 7 files
- `cd noesis && npm run test` — diagrams.test.tsx: 21 tests passing, no failures
- `cd noesis && npm run build` — TypeScript compiles, no type errors in diagram files
</verification>

<success_criteria>
- 7 diagram TSX files exist in noesis/src/components/diagrams/
- Each exports a named function component (not default export)
- Each SVG uses CSS variables (var(--color-accent-blue) etc.) — no hardcoded hex values
- Each SVG has role="img" and aria-label
- Each SVG uses viewBox and className="w-full max-w-4xl mx-auto my-8" for responsive sizing
- diagrams.test.tsx: 21 tests passing green
- npm run build: TypeScript exits 0, no compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-content/02-03-SUMMARY.md`
</output>
